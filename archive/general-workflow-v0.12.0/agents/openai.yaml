interface:
  display_name: "General Workflow"
  short_description: "Batch-local and aggregate solution delivery"
  default_prompt: "Use $general-workflow to classify solution scope before feature merging, maintain separate construction and progress sources for every batch plus root aggregate progress, then close one Delivery Anchor gap through the real production path."

policy:
  allow_implicit_invocation: true
