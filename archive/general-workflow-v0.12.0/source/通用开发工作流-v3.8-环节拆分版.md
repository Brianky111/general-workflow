# 通用开发工作流 v3.8：需求分层 × 接口先行 × 测试先行（Agent 协作版）

> **用途**：任何新项目的基础流程。复制进 CLAUDE.md 后按文末「微调点清单」调整。
> **角色约定**：「主导者」= 提出需求、审核中文文档与界面的人（不读实现代码）；「agent」= 执行编码的 AI。
> **v3 变更（在 v2.1 基础上）**：治理范围修正（放行进度/fixtures）与守门设施自护、红证据回放闭环、验收场景沉淀为自动化回归、验证强度三档（标准/强化/对抗）与性质测试、反例回收零容忍（可复现即必修）、fixture 保鲜、功能分级轻量道、初审新增断言强度抽查、反问只问意图不外包设计。
> **v3.1 增补**：补齐编排层——边界规则（只排顺序不做判断）、剧本测试（判断工具升为四问）、编排层测试的断言对象。
> **v3.2 增补**：清除模板内残留的具体业务示例，改为抽象判别标准 +〈占位〉形制；示例实例化交给项目 agent。
> **v3.3 增补**：新增推进模式（蓝图/增量）——蓝图模式下全部需求、全部接口总审冻结后才开始实现；反问升级为带硬停点的强制产物，禁止同次运行越过停点。
> **v3.4 增补**：新增模糊审计 agent——停点后由独立 agent 冷读文档，以追溯检查拆穿静默假设，分诊「文档缺陷打回」与「真模糊并入反问清单」。
> **v3.5 修正**：修正接口文档粒度冲突、收窄反问门禁范围、补上实现 commit 弱化测试的防线、拆分受审 fixture 与机器生成缓存、修正 A 级变更顺序，并把红回放从“全部测试失败”改为“标记红用例以预期原因失败”。
> **v3.6 增补**：新增大需求/大功能拆分规则——默认一份功能级接口文档；满足模块数、并行实现、外部协议、强化/对抗档、复用等触发条件时，再拆为 `interfaces/<模块名>.md`。
> **v3.7 增补**：补齐运行设施——Git hooks / agent hooks 的职责边界、项目级与功能级状态文件、状态模板、阶段流转更新点；明确 hooks 与状态文件只做提前反馈和导航，过关权威仍是 Git/PR/CI。
> **v3.8 增补**：按环节拆分阶段 0–2——先判断新/旧项目，再分别封存原始需求与整理后需求；接口层新增代码现状冲突/功能重叠记录；规划层必须消费冲突记录并明确从零实现、复用扩展、修改现有代码或重构后实现。
> **模板纪律**：本模板保持领域无关——只写判断标准与格式形制，不预置任何业务示例。文中〈尖括号〉为占位符，由项目 agent 用本项目的领域内容实例化，并在对应关卡经主导者确认。

---

## 核心原则（不可微调）

1. **合同先行**：接口文档是唯一真相。代码服从文档，不是文档追认代码。
2. **测试先行**：任何实现代码写下之前，必须已存在「运行过且失败（红）」的测试。
3. **人审文档，机审代码，人眼审界面**：人工关卡设在中文文档与可视界面上；实现代码由测试、CI 与初审 agent 验证。
4. **大声失败**：错误必须抛出或上报。禁止默认值兜底、空 catch 吞错、让流程静默继续。
5. **冻结靠机制不靠自觉**：文档的「冻结」由 Git 治理强制（CODEOWNERS + 分支保护 + CI 守门），不是口头约定。
6. **仓库即记忆**：需求、合同、规划、状态、进度全部外置到 `docs/`。agent 会话会消失，任何新 agent 开工第一步是读文档目录、状态文件与进度文件。
7. **可复现即必修**：任何带输入或种子记录的失败——反例、场景红、变异存活——都是必须清偿的债务。唯一两条出路：修实现，或走变更协议改合同。禁止忽略、重试蒙混、skip 掩盖。

## 仓库布局（固定约定）

```
docs/
├── architecture.md              # 项目级架构：层级地图、依赖方向、目录结构（开工时定一次）
├── glossary.md                  # 术语表：中文领域词 ↔ 英文标识符（所有 agent 复用）
├── requirements-index.md        # 需求总清单：全部功能一句话 + 分级 + 状态（蓝图模式的总控台）
├── workflow-state.json          # 机器可读项目状态镜像；只导航，不作过关权威
├── domain-models.md             # 共享数据模型：跨功能复用的实体只在此定义一处，各接口文档引用
└── features/<功能名>/
    ├── 00-项目识别.md           # 新项目/旧项目/旧项目中新模块的判断与依据；不做代码冲突结论
    ├── 00-原始需求.md           # 主导者原话、附件、上下文；原则上只追加不改写
    ├── 00-整理后需求.md         # agent 结构化后的需求、边界、编号验收场景、反问与决策记录
    ├── 01-接口.md               # 默认功能级合同；大需求拆分时降为索引，不写方法细节
    ├── 01-代码冲突与重叠.md     # 旧项目必填：现有代码与目标需求/接口的冲突、重叠、风险；新项目写“不适用”
    ├── interfaces/              # 可选：大需求/并行实现时启用
    │   └── <模块名>.md          # 单模块合同：数据模型、方法四问、不变量声明、场景走查
    ├── conflicts/               # 可选：冲突很多或多 agent 并行时启用
    │   └── <模块名>.md          # 单模块代码冲突/功能重叠清单，汇总入口仍是 01-代码冲突与重叠.md
    ├── 02-规划.md               # 功能级规划：实现方式决策、冲突处理、方法归属、验证强度标注
    ├── status.json              # 机器可读功能状态镜像；与 99-进度.md 保持一致
    ├── fixtures/
    │   ├── contract/            # 探针实录的外部数据样本，受审，禁止随意改写
    │   ├── counterexamples/     # 反例输入，append-only，删除/改写需走变更协议
    │   └── generated/           # 可再生成的保鲜报告/缓存，允许机器写入
    └── 99-进度.md               # 各模块状态。按模块分节，每个 agent 只改自己模块的小节，避免并行冲突
```

## 文档治理（Git 机制）

- **文档与代码同仓库同分支**。禁止长期独立的文档分支——文档与代码必须在同一条时间线上，「先改文档 → 再改测试 → 再改实现」的顺序才可审计。
- **任何受治理文档的新增或修改走文档 PR**：短命分支 → PR → 主导者批准 → 合并。**合并进 main = 冻结生效**。文档 PR 的 diff 是中文 Markdown 的 diff，正好是主导者审得动的产物。本文把 `architecture.md`、`domain-models.md`、`00-项目识别.md`、`00-原始需求.md`、`00-整理后需求.md`、`01-接口.md`、`01-代码冲突与重叠.md`、`interfaces/*.md`、`conflicts/*.md`、`02-规划.md` 归为受治理文档；其中只有需求层与接口合同强制适用反问硬停点。`workflow-state.json`、`status.json`、`99-进度.md` 属于状态镜像，不是冻结或过关权威。
- **保护范围要精确**——锁合同，放行机器高频写入路径。CODEOWNERS 示例（GitHub 规则：后匹配覆盖前匹配，空所有者行 = 解除必审）：

  ```
  # 受治理文档、合同 fixture 与守门设施 —— 主导者必审
  /docs/**                        @主导者
  /.github/**                     @主导者
  /eslint.config.*                @主导者
  /scripts/audit/**               @主导者

  # 高频机器写入路径与 CI 约束路径 —— 后置空规则放行
  /docs/workflow-state.json
  /docs/features/*/status.json
  /docs/features/*/99-进度.md
  /docs/features/*/fixtures/counterexamples/
  /docs/features/*/fixtures/generated/
  ```

  状态文件、进度文件、反例追加区与机器生成缓存不靠人眼守：状态/进度只是登记与导航，不能作为冻结或过关权威；`fixtures/counterexamples/` 由 CI 强制 append-only；`fixtures/generated/` 只是可再生成报告。`fixtures/contract/` 是合同证据，默认受审；真实性再由「保鲜复测」（见定时任务）对照外部实况把守。
- **守卫者也需要守卫**：一切能削弱守门的文件——CI workflow、lint 配置、审计脚本、CODEOWNERS 自身——全部列入主导者必审范围。否则写码 agent 可以先改弱守门再过关。
- **三个平台开关**（项目开工时设置一次）：
  1. **CODEOWNERS**：如上，agent 物理上无法擅自合并受保护路径。
  2. **分支保护**：main 禁止直接 push、禁止 force push——修改记录不可抹除。
  3. **CI 守门**：`feat` / `fix` 类 PR 若改动受治理文档或合同 fixture，直接判红，逼其走文档 PR 或变更协议；反例 fixture 只许追加、禁止修改/删除；`fixtures/generated/` 除外。
- **commit 规范**：文档改动用 `docs(识别|需求|接口|规划|架构): 描述`，涉及变更提案的注明编号。
- **降级方案**（个别仓库无远端 PR 流程时）：主导者每次批准即打 tag（如 `<功能名>-contract-v3-approved`），初审 agent 固定 diff 当前 `docs/` 与最近批准 tag，有漂移即违规。此模式为「可查不可防」，仅作过渡。

## 自动化守门（CI，项目开工时配置一次）

能脚本化的检查一律进 CI，不占用 agent 审核，也不依赖 agent 自觉。

### PR 门禁（每次 PR 必跑）

| 检查项 | 工具 |
|---|---|
| 层边界（禁止的 import 方向） | ESLint no-restricted-imports / dependency-cruiser |
| 违禁词扫描（外部原始字段外泄） | CI 内 grep 脚本 |
| 静默失败（空 catch、无声 fallback） | ESLint 规则 |
| commit 顺序 + 变更范围纯净性（test 先于 feat；test commit 只许触碰测试路径并追加反例 fixture；feat/fix/refactor commit 默认禁止触碰测试路径与合同 fixture，且不得修改/删除反例 fixture） | git log 审计脚本 |
| 反例 fixture append-only（`fixtures/counterexamples/` 只许新增文件，修改/删除旧反例直接判红） | git diff 审计脚本 |
| **红回放**（检出每个 test commit，运行本次新增或标记的红用例，断言它们以预期原因失败；既有测试不得出现非目标崩溃） | CI 红回放 job |
| 合同一致性（文档签名 vs 代码签名） | 签名比对脚本 |
| 反问存在性 + 清零（仅需求与接口合同文档强制检查：缺失「待确认反问 / 决策记录」区，或含未答复项，均判红；其他受治理文档按各自模板检查） | CI 内标记扫描脚本 |
| 模糊审计报告存在（需求与接口合同文档 PR 须附审计报告；架构/规划类文档按关卡要求附审查记录） | CI 文件存在性检查 |
| commit 信息格式 | commitlint（Conventional Commits） |
| 状态一致性（`workflow-state.json`、`status.json`、`99-进度.md` 与真实 PR/CI/文档状态不矛盾） | 状态校验脚本 |
| 全量单测 + 性质测试（每次换新随机种子；失败种子自动记录保证可复现） | 测试框架 + fast-check |
| 集成场景回归（历史功能的编号场景自动化测试全绿） | CI E2E job |
| 视觉回归（人眼轨截图对比基线） | Playwright 截图对比 |

红回放补上时序漏洞：只查 commit 顺序时，agent 可以先在本地写好实现、再补一个「从未真红过」的测试。红回放 + commit 范围纯净性联手后，「红」成为机器验证的历史事实，不再依赖 agent 贴图自觉。红回放只断言本批新增/标记红用例以预期原因失败，不要求整套测试“全部失败”，避免既有绿测试被误伤。

### 定时任务

| 任务 | 说明 |
|---|---|
| fixture 保鲜 | 定期重跑探针，diff 外部实况与 `fixtures/contract/`；漂移即告警并触发 A 级变更（外部行为变 = 合同基础变） |
| 夜间深度轰炸 | 大预算性质测试 / fuzz 持续攻击已合并代码；发现的失败连同种子进反例回收 |
| 变异测试 | 对强化 / 对抗档模块跑 Stryker：故意改坏代码后测试仍全绿的「存活变异」= 弱断言债务，逐个补测试或书面说明豁免理由 |

## 运行设施：hooks 与状态文件（项目开工时配置一次）

hooks 与状态文件的作用是**提前提醒、减少遗漏、方便交接**；它们不是安全边界。真正的过关权威仍然是 Git 历史、PR 审批、分支保护、CI 记录与冻结后的合同文档。任何状态文件写着“已完成”，但找不到对应 PR/CI/commit 证据，一律按未完成处理。

### 状态文件

| 文件 | 谁写 | 用途 | 权威性 |
|---|---|---|---|
| `requirements-index.md` | agent 起草，主导者审 | 项目/蓝图级功能总控台：范围、分级、状态、候补区 | 人审后的范围依据 |
| `workflow-state.json` | hooks/agent 更新，CI 校验 | 机器可读项目状态镜像：推进模式、当前关卡、冻结 ref、待答复数 | 只导航，不作过关依据 |
| `features/<功能名>/status.json` | hooks/agent 更新，CI 校验 | 机器可读功能状态：阶段、模块状态、证据链接、阻塞项 | 只导航，不作过关依据 |
| `features/<功能名>/99-进度.md` | 各模块 agent 只改自己小节 | 人读交接：当前进度、下一步、阻塞、证据摘要 | 交接依据，不作冻结依据 |

状态文件不得保存密钥、账号、token、外部系统完整敏感响应；只能保存路径、commit、PR、CI 链接、短摘要与可公开的状态。

`workflow-state.json` 最小字段：

```json
{
  "schema": 1,
  "mode": "blueprint|incremental",
  "currentGate": "kickoff|identification|requirements|interfaces|planning|implementation|review|integration|done|blocked",
  "inScopeFeatures": ["<功能名>"],
  "lastApprovedRef": "<main-sha-or-approved-tag>",
  "pendingQuestions": 0
}
```

`features/<功能名>/status.json` 最小字段：

```json
{
  "schema": 1,
  "feature": "<功能名>",
  "projectType": "new|existing|new-module-in-existing",
  "phase": "identification|requirements|interfaces|planning|red|green|review|integration|done|blocked",
  "gate": "open|waiting-human|approved|blocked",
  "pendingQuestions": 0,
  "requirementsConfirmedAt": "<pr-or-tag-or-main-sha>",
  "contractsFrozenAt": "<pr-or-tag-or-main-sha>",
  "conflictReport": "01-代码冲突与重叠.md",
  "modules": {
    "<模块名>": {
      "owner": "<agent-id-or-branch>",
      "status": "todo|red|green|review|done|blocked",
      "contract": "01-接口.md#<模块名>",
      "redEvidence": "<ci-run-or-commit>",
      "greenEvidence": "<ci-run-or-commit>",
      "next": "<下一步一句话>"
    }
  }
}
```

`99-进度.md` 每个模块小节固定形制：

```markdown
## <模块名>
- 状态：todo / red / green / review / done / blocked
- 负责人：<agent-id 或分支名>
- 合同：<01-接口.md#锚点 或 interfaces/<模块名>.md>
- 红证据：<CI 链接或 commit；无则写“无”>
- 绿证据：<CI 链接或 commit；无则写“无”>
- 阻塞：<待确认反问 / 失败测试 / 外部依赖 / 无>
- 下一步：<一句话>
```

### Git hooks（本地提前失败）

Git hooks 由仓库提供脚本，但不假设开发者一定安装；CI 必须重复执行同等检查。推荐把脚本放在 `scripts/hooks/`，用 `git config core.hooksPath scripts/hooks` 启用。

| hook | 建议动作 |
|---|---|
| `pre-commit` | 扫 `skip/only`、空 catch、无声 fallback；检查 `fixtures/counterexamples/` 只追加；阻止非生成路径的明显越权改动 |
| `commit-msg` | 检查 Conventional Commits；根据 diff 校验 `docs/test/feat/fix/refactor` 类型与路径匹配 |
| `pre-push` | 跑快速测试、状态一致性脚本、待确认反问扫描；若代码早于合同冻结或反问未清零则阻止推送 |
| `post-checkout` / `post-merge` | 提醒重读 `workflow-state.json`、当前功能 `status.json` 与 `99-进度.md`；必要时重新生成 stub 或刷新依赖 |

### Agent hooks（平台支持时）

如果使用的 agent 平台支持会话/工具 hooks，可把下面规则做成强制提示或阻断；不支持时，写入 `CLAUDE.md` / `AGENTS.md` 作为开工清单。

| 时机 | 动作 |
|---|---|
| 会话开始 | 先读 `architecture.md`、`requirements-index.md`、`workflow-state.json`、目标功能的 `00-项目识别.md`、`00-原始需求.md`、`00-整理后需求.md`、`01-接口.md`、`01-代码冲突与重叠.md`、`02-规划.md`、`status.json`、`99-进度.md`；不存在的按当前阶段跳过 |
| 写文件前 | 若要写接口，检查需求是否已确认；若要写规划，检查冲突记录是否已完成；若要改代码，检查合同与规划是否冻结；若要跨阶段写文件，检查硬停点是否已解除 |
| 生成反问后 | 自动停止本次运行，只允许更新反问清单、状态文件与进度文件，禁止继续写接口/规划/代码 |
| 提交前 | 更新 `status.json` 与 `99-进度.md`，填入红/绿/审查证据；证据缺失时不得标记 done |
| 会话结束 | 写清下一步、阻塞项、已跑命令、未跑验证，方便下个 agent 接手 |

### 状态一致性规则

- `pendingQuestions` 必须等于文档中未答复 `【答复】：` 的数量；不一致时 CI 判红。
- `requirementsConfirmedAt` 必须能解析到主导者确认过的需求 PR、main commit 或批准 tag；空写“已确认”无效。
- `contractsFrozenAt` 必须能解析到已合并 PR、main commit 或批准 tag；空写“已冻结”无效。
- `projectType=existing|new-module-in-existing` 时，`conflictReport` 必须指向存在的 `01-代码冲突与重叠.md`，且扫描结论不能缺失。
- 模块 `status=done` 必须同时有合同引用、红证据、绿证据、初审报告或集成报告引用；缺一项降级为 `blocked` 或 `review`。
- 状态文件允许滞后，但不允许超前：可以还没更新 done，不能没有证据却写 done。
- 并行 agent 只改自己模块的 `99-进度.md` 小节；跨模块状态变更写入 `status.json` 时必须保留其他模块字段。

## 推进模式（项目级节奏，先于一切功能工作选定）

同一套阶段，两种推进节奏。**新项目 / 大版本默认蓝图模式；已上线蓝图之上的新增与修补默认增量模式。**

### 蓝图模式：全部合同冻结，寸码不动

节奏是「分阶段推土机」，不是「单功能流水线」——每个阶段对**全部功能**批量完成并总审，才进入下一阶段：

1. **需求总集**：对全部功能批量执行阶段 0，产出各功能的 `00-项目识别.md`、`00-原始需求.md`、`00-整理后需求.md` + `requirements-index.md` 总清单。全部草稿完成后，模糊审计 agent 冷读全集并分诊，真模糊与起草 agent 自报的问题汇总为**一份合并反问清单**（按功能分组），主导者一次答复 → **需求总审关卡**。
2. **接口总集**：对全部功能批量执行阶段 1，并为旧项目或旧项目中新模块产出 `01-代码冲突与重叠.md`。跨功能复用的实体抽入 `domain-models.md` 只定义一处，各功能级接口文档或拆分后的模块接口文档引用。全部草稿完成后，由模糊审计 agent 一并执行**跨合同一致性检查**——同名字段同含义（对照术语表与共享模型）、无重复或冲突的方法、共享模型无本地私改（字段比对可脚本化）——与全集模糊审计，真模糊汇入合并反问清单 → **接口总审关卡**，通过即**全部合同一次性冻结**。
3. **全局规划**：阶段 2 对全部功能一次做完（实现方式决策、冲突处理、方法归属、验证档位）→ **规划总审关卡**。
4. 此后阶段 3–5 才开闸，多 agent 按功能与模块大规模并行；各功能完成后分别走关卡③验收。

两条配套纪律：外部触点必须在接口总审前**全部**探针实录完毕——蓝图模式最大的风险就是对着想象中的外部世界签合同；同时要认清代价：设计错误发现得越晚，A 级变更越多，蓝图模式是用变更成本换**合同一致性**与**主导者的批量审核效率**，在 agent 实现速度快的前提下通常划算。

### 增量模式：单功能循环

即「每个功能（循环）」流程，逐功能走阶段 0–5。轻量道、B/C 级变更都属于此模式。

文件粒度两种模式完全一致（一功能一档），差别只在推进顺序与关卡组织：蓝图模式把散点关卡合并成三次总审。

## 功能分级（agent 判断，主导者确认）

不是所有功能都值得全套仪式。**增量模式下**，agent 在开工时**自行判断并提议档位**，理由写在功能文档头部，主导者在首个文档 PR 一并确认或改判（蓝图模式由总集统一批量推进，无需分道）：

- **标准道**：默认。完整走阶段 0–5。
- **轻量道**：同时满足「无新外部依赖、涉及模块 ≤ 2、不含高错误代价判定（标准同验证强度触发信号）」的小功能。原始需求仍单独封存；整理后需求、接口、规划可合并为一份 `00-功能.md`，其中接口内容仍按模块小节写清方法四问；若是旧项目，必须保留代码冲突/重叠小节；关卡①②合并为一次文档 PR 审；实现、初审、防作弊铁律照旧不减；集成场景并入既有回归即可。

bug 修复不在此列——直接走变更协议的 B 级微流程。

### 大需求拆分（文档粒度，不是新分级）

“大需求/大功能”指一个功能已经跨过多个稳定边界，继续塞进单份接口文档会让合同难审、难测、难并行。它仍然可以是标准道或强化/对抗档；“大”只决定文档粒度与并行组织方式。

需求确认后，默认先写一份 `01-接口.md`。满足以下任一触发信号时，再拆为 `interfaces/<模块名>.md`；若代码冲突项也明显膨胀或需要并行处理，可同步拆为 `conflicts/<模块名>.md`，但 `01-代码冲突与重叠.md` 仍保留汇总入口：

1. **稳定模块 ≥ 3 个**：每个模块能独立测试、独立失败、独立替换。
2. **需要多 agent 并行实现**：为减少冲突，每个 agent 认领一个模块合同与进度小节。
3. **涉及外部协议或外部系统**：需要探针、合同 fixture、解析失败语义，适配层合同应单独成文。
4. **存在强化/对抗档模块**：性质测试、不变量、反例回收、攻击输入需要更清晰的模块合同。
5. **合同明显膨胀**：方法数过多、场景走查过长，或单份接口文档超过项目约定阈值（如约 300 行；阈值在微调点里定）。
6. **模块会被多个功能复用**：共享能力独立成合同，避免复制粘贴到多个功能文档。

拆分后，`01-接口.md` 降为**功能级索引**：只写模块列表、共享模型引用、跨模块调用顺序、阅读顺序与总场景走查入口；每个方法的四问、字段表、不变量与失败语义必须落到对应 `interfaces/<模块名>.md`。禁止索引和模块文档重复定义同一字段或方法；共享实体只放 `domain-models.md`。

推荐拆分形制：

```text
features/<功能名>/
├── 01-接口.md                 # 索引：模块地图 + 跨模块走查入口
├── interfaces/
│   ├── <外部适配模块>.md       # 探针 fixture、原始字段、解析失败
│   ├── <核心映射模块>.md       # 领域规则、不变量、性质测试蓝本
│   ├── <配置生成模块>.md       # 输出合同、序列化/反序列化规则
│   ├── <编排模块>.md           # 调用顺序、中止/重试/回滚
│   └── <界面契约模块>.md       # 页面状态、错误提示、人眼验收点
└── conflicts/
    └── <模块名>.md             # 可选：该模块的现有代码冲突、功能重叠与风险
```

不满足触发信号时，不要为拆而拆：保留单份 `01-接口.md`，按模块用二级标题分节即可。

## 流程总览

**项目开工（一次性）**：架构文档 + 治理开关 + CI 守门 + hooks/状态文件 + 术语表初始化

**推进节奏**：蓝图模式 = 需求总集 → 接口总集（跨合同一致性检查 + 代码冲突/重叠汇总）→ 全局规划（消费冲突记录）→ 三次总审全过后并行实现；增量模式 = 下图单功能循环。

**每个功能（循环）**：

```
功能分级 → 阶段0.0 项目识别（新/旧项目）
→ 阶段0.1 原始需求封存 → 阶段0.2 整理后需求 → 反问对齐 → 需求确认
→ 阶段1 接口合同 + 代码冲突/重叠记录 → 反问对齐 →【文档PR·关卡①】
→ 阶段2 实现规划（读取冲突记录，决定从零/复用/修改/重构）【文档PR·关卡②】
→ 阶段3 红绿实现（按模块）→ 阶段4 模块初审（初审agent）
→ 阶段5 集成测试 + 人眼验收【关卡③】→ 场景固化为自动回归
```

| 阶段 | 产物 | 审核者 | 审什么 |
|---|---|---|---|
| 开工 | architecture.md + 治理配置 | 主导者 | 层级地图 + 样例沿层走查 |
| 0.0 识别 | `00-项目识别.md` | agent 自检，主导者可抽看 | 新项目 / 旧项目 / 旧项目中新模块的判断是否有依据 |
| 0.1 原始需求 | `00-原始需求.md` | 主导者 | 原话是否被完整封存、未被改写 |
| 0.2 整理需求 | `00-整理后需求.md` + 反问清单 | 模糊审计 agent → 主导者 | 审计分诊 → 答复反问 → 确认编号场景 = 我的意图 |
| 1 接口 | `01-接口.md`（大需求可拆 `interfaces/<模块名>.md`）+ `01-代码冲突与重叠.md` + 不变量 + 受审 fixtures + 反问清单 | 模糊审计 agent → 主导者（文档 PR） | 接口是否表达目标行为；现有代码冲突/重叠是否讲清楚 |
| 2 规划 | `02-规划.md`（含实现方式决策、冲突处理、验证强度标注） | 主导者（文档 PR） | 冲突是否逐项处理；实现方式、沿层走查、轨道与档位是否合理 |
| 3 实现 | 测试 + 实现 + CI 记录 | CI | 防作弊铁律 |
| 4 初审 | 审核报告（附证据引用） | 初审 agent 出报告，主导者读结论 | 三道判断题 |
| 5 集成 | 集成报告（按场景编号，附归因）+ 界面演示 | 审核 agent + 主导者 | 场景全绿 + 人眼过界面 |

---

## 项目开工（一次性）

1. **架构文档 `architecture.md`**，必备章节：
   - 层级地图：每层一行 `层名 / 一句话职责 / 禁止事项`
   - 依赖方向：单向依赖图。上层可调下层；下层不知道上层存在。
   - 目录结构：目录树 + 每个目录一句话
2. **分层按项目定，不是固定套路**。层数与层名（前端层 / 消费层 / 编排层 / 抽象层 / 核心层 / 适配层……叫法自由）不是重点，边界规则才是。用四问决定要哪些层：
   - 有需要隔离的外部系统或第三方 API 吗？→ 设**适配层**，外部原始格式禁止越过此层
   - 有与界面无关的纯业务规则吗？→ 设**核心层**，不 import 任何外部/UI 代码，可纸上演算
   - 有多步骤流程要串联吗？→ 设**编排层**，只排顺序、不做判断：串接各层调用，决定某步失败时中止/重试/回滚。分支判别标准：**需要领域知识才能回答的分支属于核心层**；编排层的分支只许依据下层返回的结论（成功/失败/核心层给出的判定结果）。它是唯一允许同时看见核心层与适配层的层
   - 有人机界面吗？→ 设**前端层**，只做展示与转发，禁止写业务规则
   没有对应需要的层就删掉，别为分层而分层。
3. **判断工具四问**（写进架构文档，规划与初审通用；**写入架构文档时每问必须附 1–2 个本项目示例**，开工关卡一并确认）：
   - 平台替换测试（对准适配层）：把外部系统换成另一家，哪些代码要改？要改的都该在适配层。
   - 纸上演算测试（对准核心层）：核心层函数能否只靠输入在纸上算出输出？不能就是混入了外部依赖。
   - 剧本测试（对准编排层）：编排函数能否用「第一步调谁、第二步把结果交给谁」的编号清单讲完？讲述中一旦出现需要领域知识才能判定走向的条件——答案写在业务合同里、而不在下层返回值里——就是规则漏进了编排层，搬回核心层。
   - 大声失败三问（通用，编排层是主战场）：这里出错会抛出吗？会被谁看到？还是被默认值悄悄吞掉？
4. 配置文档治理三开关、CI 守门与运行设施（含 hooks、状态文件、红回放、场景回归、定时任务）。
5. 初始化 `glossary.md`。
6. **关卡**：主导者审层级地图 + 一条样例场景的沿层走查。批准合并后架构冻结，此后架构改动走级别 A 变更。

## 阶段 0：项目识别与需求创建

阶段 0 只回答两个问题：「这是在什么项目状态下提出的需求？」和「主导者到底想要什么？」。本阶段**暂不做代码冲突判断，不用现有代码否定或裁剪需求，也不决定实现方式**。即使 agent 看到现有代码可能冲突，也只能标记“待接口层验证”。

### 0.0 项目识别：新项目还是旧项目

进入任何功能需求前，agent 必须先创建 `00-项目识别.md`，判断当前工作属于哪类：

- **新项目**：仓库没有可复用的产品代码，或本期从空白工程开始；后续接口层通常无需代码冲突扫描，但仍要检查架构与外部约束。
- **旧项目**：仓库已有运行中的代码、历史接口、数据结构、测试、文档或用户已依赖的行为；后续接口层必须做代码现状扫描。
- **旧项目中的新模块**：功能本身是新增能力，但会嵌入既有客户端、服务端、插件、配置、UI 或发布流程；按旧项目处理，因为集成点会产生冲突和重叠。

`00-项目识别.md` 固定形制：

```markdown
# 00-项目识别

## 结论
- 类型：新项目 / 旧项目 / 旧项目中的新模块
- 判断依据：<仓库状态、已有文档、已有入口、已上线行为等事实>

## 已读资料
- <文档路径或代码入口；只列事实>

## 后续接口层必须检查的区域
- <现有 API / 数据模型 / UI 流程 / 外部适配 / 配置 / 测试 等>

## 本层不做
- 不判断需求是否“应该改成适配旧代码”
- 不决定从零实现还是改旧代码
- 不处理代码冲突；冲突留到 `01-代码冲突与重叠.md`
```

### 0.1 原始需求封存

创建 `00-原始需求.md`，保存主导者的原始输入。它是需求溯源，不是整理稿。

规则：

1. **原话保留**：保留用户原句、截图/附件路径、上下文链接、时间；不得润色、合并、删除语气词或把口语改成正式需求。
2. **只追加不改写**：用户后续补充写成新条目；如果原始记录确实有误，用“更正记录”说明，不覆盖旧文本。
3. **不做技术解释**：本文件不写代码判断、实现推断、可行性评价。
4. **可追溯**：`00-整理后需求.md` 的每个目标、非目标、验收场景都应能追溯到原始需求条目或后续决策记录。

### 0.2 整理后需求

创建 `00-整理后需求.md`。它是 agent 对原始需求的结构化版本，用来让主导者确认“这是不是我的意思”。

必备章节：

1. **功能一句话**：用业务语言说明要达成什么。
2. **目标与非目标**：明确做什么、不做什么；非目标必须能追溯到原始需求、反问答复或主导者显式确认。
3. **用户/使用场景**：谁在什么上下文里使用。
4. **编号验收场景**：强制分三类：正常路径（S1、S2…）、异常路径（E1、E2…）、边界情况（B1、B2…），格式 `给定…，当…，应该…`。
5. **功能分级提议**：标准道 / 轻量道及理由；大需求拆分只做风险提示，不在此处拆接口。
6. **待接口层验证项**：如果 agent 怀疑与现有代码、接口、数据或流程冲突，只能写“待接口层验证”，不得在需求层下结论。

编号贯穿全流程：接口、测试描述、集成报告都引用编号，漏测审计变成机械比对（缺号即漏测）。

### 需求确认关卡

需求层出口条件：

- `00-项目识别.md` 已说明新/旧项目判断与依据。
- `00-原始需求.md` 已完整封存主导者输入。
- `00-整理后需求.md` 已把原始输入整理为目标、非目标、编号场景和待确认反问。
- 模糊审计已完成，`待确认反问` 区清空。
- 主导者明确确认：`00-整理后需求.md` 表达的是自己的意图。

只有通过需求确认关卡后，agent 才能进入接口层读取代码现状并判断冲突。

### 反问对齐（收尾必经）

起草前的澄清问的是「我缺什么信息才能动笔」；起草后的反问问的是「成文过程暴露了哪些主导者没说、agent 不能替他决定的点」。隐藏的模糊往往只有在试图把需求写成文时才现形，所以两轮性质不同，都不可省。

规则：

1. 草稿完成后，agent 必须输出**反问清单**（`00-整理后需求.md` 内 `## 待确认反问` 区，编号 Q1、Q2…）。数量随模糊程度走，不设上限，按阻断程度排序。声明「无反问」必须附**逐类自证**——正常/异常/边界场景、字段含义、失败语义各自为何已无可问——主导者可打回；禁止用一句「无反问」跳过。
2. **硬停点**：输出反问清单即为本次任务的终点。agent 必须就此停止运行，禁止在同一次运行中继续写接口、规划或代码；答复到达后才开启下一段任务。交互会话中可以用提问面板呈现同编号的问题，但机制以文档内清单为准——面板是便利，文档才是通道，不依赖 agent 是否「想起来」弹面板。
3. 蓝图模式下，各阶段的反问跨功能汇总为一份合并清单（按功能分组），主导者一次答复。
4. 每条反问必须做到三点：**定位**（指向具体场景编号或字段，不许空泛）、**给选项**（2–3 个候选答案及各自后果）、**附建议**（agent 推荐哪个、为什么）。禁止「还有什么要补充吗」式开放空问。
5. **只问意图，不外包设计**：「要不要支持 X」「X 与 Y 冲突时以谁为准」这类意图题必须问；「用哪个库」「函数怎么拆」这类技术题由 agent 自决并写入决策记录留痕，不得抛给主导者。
6. **禁止静默假设**：发现模糊点唯一出路是进反问清单。因为附了建议答案，主导者可批量答复（如「Q2 选 B，其余按建议」），效率不受影响。
7. 答复写回正文后，反问连同结论移入 `## 决策记录` 区——留下「当初为什么这么定」的痕迹，供后续 agent 与变更协议引用。
8. 答复若催生新场景，补进 S/E/B 编号清单。
9. **出口条件**：模糊审计已完成 + `待确认反问` 区清空 + 主导者最终确认「这份场景清单 = 我的意图」，方过关卡。

### 模糊审计 agent（第二道网，关卡前置）

起草 agent 只能报出它**意识到**的模糊；静默假设的定义恰恰是作者没意识到的假设，靠自报天然不完备。所以停点之后、清单交主导者之前，由独立的**模糊审计 agent** 冷读文档——只给 `00-原始需求.md`、`00-整理后需求.md`、上游文档、术语表、共享模型，不给起草过程的任何上下文，也不使用代码现状替主导者改需求——按清单扫描：

- 原始需求里的明确意图是否都进入整理稿
- 未定义或前后不一致的术语（对照 `glossary.md`）
- 未闭合的分支：提到「要处理〈某情况〉」却没说处理成什么样；S/E/B 三类中留白的类别
- 量词与程度词模糊：未量化的「快、慢、大量、及时」类表述
- 边界未定：取值范围、默认值、空值与极端输入的行为缺失
- 冲突：文档内两句话不能同真，或与上游文档、共享模型对不上
- **追溯检查（拆静默假设的核心）**：整理稿里每一条行为都必须能追溯到原始需求、场景编号或决策记录；追溯不到的内容 = 起草 agent 自己编的 = 疑似静默假设，必须变成反问

审计结果分诊两路：**文档缺陷**（漏章节、缺示例、违反模板纪律）直接打回起草 agent 修，不烦主导者；**真模糊**（需要主导者意图才能定）并入 `待确认反问` 区，与起草 agent 自报的问题合并去重。审计报告随文档一并提交，每项发现附文档位置引用——无证据 = 未审。

反问模板：

```markdown
### Q1【场景/字段编号】〈一句话点出模糊点〉
- A. 〈候选答案〉——〈后果〉
- B. 〈候选答案〉——〈后果〉
- 建议：〈A 或 B〉——〈理由〉
- 【答复】：
```

## 阶段 1：接口层：合同 + 代码现状冲突记录【文档 PR · 关卡①】

接口层在需求确认之后开始。它回答两个问题：「目标行为的合同是什么？」和「这些目标行为与现有代码、接口、数据、流程有没有冲突或重叠？」

默认产物 = `01-接口.md` + `01-代码冲突与重叠.md`。若命中“大需求拆分”触发信号，则产物 = `01-接口.md` 功能级索引 + `interfaces/<模块名>.md` 模块合同集合；若冲突项也过多或需要并行处理，可增加 `conflicts/<模块名>.md`，但汇总入口仍是 `01-代码冲突与重叠.md`。

### 入口条件

- `00-项目识别.md` 已完成，并明确新/旧项目判断。
- `00-原始需求.md` 已封存。
- `00-整理后需求.md` 已经主导者确认。
- 阶段 0 的 `待确认反问` 已清空。

### 代码现状扫描（旧项目必做，新项目写“不适用”）

旧项目或旧项目中的新模块，agent 必须读取现有代码与相关文档，记录事实，不在本层决定实现方案。

检查范围按项目实际裁剪，但至少覆盖：

- 现有公开 API、命令、事件、路由、组件入口、配置入口
- 现有数据模型、持久化结构、缓存结构、序列化格式
- 现有用户流程、错误提示、状态机、权限或开关逻辑
- 现有外部适配、协议、fixture、测试替身
- 与本功能名称、目标、场景相似的历史功能，避免重复造轮子

判断原则：

- **需求层已确认的目标行为不因为旧代码而自动降级**。旧代码只能形成冲突记录，不能静默改写需求。
- 如果冲突影响“目标行为到底应该是什么”，这是意图模糊，进入接口层反问。
- 如果冲突只是“现有代码如何改才达成目标”，记录到 `01-代码冲突与重叠.md`，留给阶段 2 规划处理。
- 如果发现已有功能可复用或重叠，也要记录；“已有类似代码”不是自动复用，复用决策在规划层做。

### 接口合同必备章节

1. **一句话职责**
2. **需求追溯**：列出本合同覆盖的 S/E/B 场景与决策记录编号
3. **数据模型表**：`| 字段 | 中文含义 | 示例值 | 来源 | 必填 |`（来源 = 外部系统原始字段 / 用户输入 / 计算得出），表后附**一条完整示例数据 JSON**
4. **方法清单**，每个方法固定四问：
   - 用途（一句话）
   - 输入（带具体示例值）
   - 输出（带具体示例值）
   - 失败（异常名 + 大声/静默声明；声明静默的必须写理由）
5. **不变量声明**（P1、P2…）：对**所有**输入都必须成立的规则，用中文陈述，形如「无论〈输入〉如何组合，〈本项目的业务底线〉必须成立」；领域无关的技术不变量同样适用（如「序列化再反序列化必须得到原对象」这类往返律）。凡触发强化/对抗档信号的模块必须声明；性质测试以此为蓝本。
6. **场景走查**：取一条编号验收场景，用同一批示例数据沿方法链从头走到尾
7. **术语表增量**：新领域词并入 `glossary.md`

### 代码冲突与功能重叠记录

`01-代码冲突与重叠.md` 固定形制：

```markdown
# 01-代码冲突与重叠

## 总结
- 扫描结论：无冲突 / 有冲突 / 有功能重叠 / 需要补充探针
- 是否阻塞接口确认：是 / 否
- 需要主导者理解的关键风险：<一句话列表>

## C1：〈一句话说明冲突或重叠〉
- 需求来源：S1 / E1 / B1 / 决策 D1 / 接口方法 M1
- 涉及现有代码：`<路径>` / `<函数或类>` / `<路由或组件>`
- 当前行为：<现有代码实际做什么>
- 目标行为：<需求或接口要求什么>
- 类型：接口签名冲突 / 数据结构冲突 / 流程冲突 / 命名冲突 / 行为冲突 / 功能重叠 / 架构边界冲突 / 外部协议冲突 / 测试 fixture 冲突
- 风险：<如果处理不好会发生什么>
- 是否阻塞接口确认：是 / 否
- 给主导者的解释：<不用代码术语也能理解的说明>
- 留给规划层：待决策；候选方向可写 2–3 个，但不在本层拍板
```

硬规则：

- 有冲突不记录 = 违规；后续实现中才“突然发现”冲突，必须补回本文件并说明为什么漏扫。
- 冲突记录必须指向具体代码位置或现有行为证据；只写“可能有冲突”不算记录。
- `01-接口.md` 写目标合同；`01-代码冲突与重叠.md` 写现状差异。二者不能混成一份“迁就现状的接口”。
- 冲突项可拆到 `conflicts/<模块名>.md`，但汇总文件必须列出全部 C 编号和状态。

### fixture 规则（数据源头）

涉及外部系统的示例数据，**必须来自探针实录**：先写一次性探针代码拿到真实返回，存入 `fixtures/contract/`；接口文档的示例与之后单元测试的 mock **都引用同一份受审 fixture**。失败反例最小化后存入 `fixtures/counterexamples/`，原则上只追加不改写；删除或改写必须走变更协议。`fixtures/generated/` 只放保鲜复测报告或可再生成缓存，不得作为合同示例的唯一来源。禁止 agent 凭想象编造外部数据的形状——mock 各自自洽但与真实世界不符，是集成阶段翻车的头号原因。fixture 的持续真实性由定时保鲜复测把守。

### 反问对齐（收尾必经，机制同阶段 0）

接口成文时暴露的新模糊点——字段默认值、两条场景的冲突、失败该大声还是静默、目标行为与旧行为冲突时主导者意图不明、任何被迫「编」出来的细节——一律进 `## 待确认反问` 区，同样定位 + 选项 + 建议。**文档 PR 的合并前置条件：模糊审计报告已附 + 待确认反问区为空**（CI 强制）。答复移入 `## 决策记录` 后，主导者再做审核，批准合并即冻结。

冻结前的疑问走反问，冻结后的疑问走变更协议——两条通道首尾相接，任何时刻都不存在「只能靠猜」的空档。

### 硬规则

- 方法没有示例数据 = 未完成，直接打回。
- 旧项目没有 `01-代码冲突与重叠.md` 或其中只写空话 = 未完成，直接打回。
- 文档 PR 合并即冻结，随后由文档生成代码 stub（签名 + 中文注释 + `throw new Error('尚未实现')`）。文档与 stub 的一致性由 CI 签名比对承担。
- 接口层禁止直接拍板“重写旧模块”“复用旧模块”“顺手重构”；这些属于阶段 2 规划。

### 主导者审核六问

1. 每个字段的中文含义我看得懂吗？
2. 示例数据沿场景走查能走通吗？有没有断链、缺字段？
3. 失败是否全部大声？声明静默的理由成立吗？
4. 不变量读起来是不是「无论输入怎样都不能坏」的底线？有没有明显缺漏？
5. 外部系统的原始字段是否只出现在适配层的文档里？（项目无外部系统则跳过）
6. 现有代码冲突或功能重叠是否讲清楚：当前是什么、目标是什么、风险是什么、为什么留到规划层？

## 阶段 2：功能规划（增量）：实现方式决策【文档 PR · 关卡②】

规划层不重新发明需求，也不重写接口。它读取上一层产物，回答：「用什么方式实现最稳？」以及「接口层记录的冲突/重叠逐项怎么处理？」

**入口条件**：`00-整理后需求.md` 已确认，`01-接口.md` / `interfaces/*.md` 已冻结，`01-代码冲突与重叠.md` 已完成且 C 编号稳定。

**不重画架构**——直接引用 `architecture.md`。产物 = `02-规划.md`：

1. **上游输入表**：列出本规划消费的需求场景、接口文档、冲突 C 编号、相关代码证据。没有被消费的 C 编号不得进入实现。
2. **实现方式判断**：必须在以下类型中选一类或组合，并写明依据：
   - **从零实现**：现有代码没有可复用能力，或复用成本/风险高于新增；需说明如何接入既有架构。
   - **修改现有代码**：目标行为与现有模块职责一致，只需调整逻辑、接口或数据结构；需说明保留哪些旧行为。
   - **复用并扩展**：已有能力大体匹配，但需要新增参数、状态、适配器或 UI；需说明扩展点和兼容策略。
   - **重构后实现**：现有代码职责混杂或层边界错误，不先整理会扩大风险；需说明重构边界、保持绿的策略。
   - **旁路替换 / 绞杀式迁移**：旧实现风险高但不能一次删除；需说明新旧并存、切换开关、回滚路径。
3. **冲突处理表**：每个 C 编号必须有处理结论。

   ```markdown
   | 冲突编号 | 处理方式 | 涉及代码 | 保留什么 | 替换/新增什么 | 测试覆盖 | 风险与回滚 |
   |---|---|---|---|---|---|---|
   | C1 | 修改现有代码 / 复用扩展 / 从零实现 / 延后并说明原因 | `<路径>` | <旧行为> | <目标改动> | S1/E1/P1 | <说明> |
   ```

4. **方法归属表**：本功能每个方法 → 所属层与模块；若方法来自拆分接口，注明接口文件。
5. **沿层走查**：一条编号场景，标注每一步发生在哪一层、跨层传递什么数据；旧项目要注明经过哪些既有入口或替换点。
6. **验证强度标注**：每个模块先分轨，红绿轨再定档。**档位由 agent 依触发信号自行判断并写明理由**，主导者在关卡②确认或改判：
   - **人眼轨**：纯展示（布局、样式、交互手感）——禁止凑数快照测试，交付截图/预览，关卡③人眼验收后录入视觉回归基线
   - **红绿轨 · 标准档**：例子式红绿（S/E/B 场景）
   - **红绿轨 · 强化档**：标准 + 性质测试（对准 P 编号不变量）+ 两两组合表（用 PICT 类工具从参数矩阵生成用例，覆盖所有两参数交互——绝大多数缺陷由一两个参数的交互触发，无需全组合）
   - **红绿轨 · 对抗档**：强化 + 独立攻击 agent（只读合同、不读实现，与写测试、写实现的 agent 三方独立）+ 变异测试达标

   触发信号（满足其一即至少强化档）：≥3 个可自由组合的输入参数；承担高错误代价或不可逆后果的判定——哪些判定属于此类，由项目 agent 依本项目领域认定并写明理由，主导者关卡②确认；解析外部输入（适配层默认 ≥ 强化）；状态机或并发逻辑。
7. 若需要新模块或触碰层边界 → 走级别 A 变更，先修订架构文档。

硬规则：

- `02-规划.md` 必须逐项消费 `01-代码冲突与重叠.md` 的 C 编号；遗漏即打回。
- 规划层可以决定“从零实现”或“改旧代码”，但必须解释为什么，不得只写结论。
- 若规划时发现新的代码冲突，先补回 `01-代码冲突与重叠.md`；若新冲突改变目标合同，退回阶段 1 或走 A 级变更。
- 规划不是实现代码。不得在规划 PR 里夹带实现。

**关卡**：主导者审「冲突处理表」+「实现方式判断」+「沿层走查」+「轨道与档位划分」是否合理。合并后规划冻结，生成全部 stub；此后多个 agent 可按模块**并行**开工。

## 阶段 3：实现——红绿循环（以模块为批次）

红绿轨的每个模块严格按以下顺序，**顺序不可倒置**：

1. **写测试（红）**：依据接口文档写该模块的全部单元测试。断言用中文并引用场景编号；性质测试引用不变量编号；依赖一律 mock，mock 数据引用 `fixtures/contract/`；反例测试引用 `fixtures/counterexamples/`。

   ```ts
   describe('〈模块名〉', () => {
     it('S1: 给定〈输入〉，应〈预期结果〉', () => { /* … */ })
     it('E1: 当〈非法输入〉时，应抛出「〈错误名〉」', () => { /* … */ })
     it('P1: 任意〈输入组合〉下，〈不变量〉恒成立', () => {
       fc.assert(fc.property(〈输入生成器〉, (输入) => { /* … */ }))
     })
   })
   ```

2. **跑红**：CI 中本批新增或标记的红用例失败，且失败原因属于「尚未实现」或目标缺陷一类；既有测试不应出现非目标崩溃。性质测试同样对 stub 必红——测试先行原则对一切测试种类生效。
3. **提交红**：测试代码单独 commit（`test: [S1,E1,P1] …`），先于任何实现 commit；**test commit 的 diff 只允许触碰测试路径，并可追加 `fixtures/counterexamples/` 中本次反例**（CI 强制）。新增外部探针样本应已在文档 PR 中进入 `fixtures/contract/`；修改/删除合同 fixture 或反例 fixture 必须走变更协议。红回放将在此 commit 上验证「红」确实发生过。
4. **最小实现（绿）**：只写让测试通过的最少代码，禁止顺手添加需求之外的功能；实现 commit 默认禁止改动测试路径、`fixtures/contract/` 与 `fixtures/counterexamples/`。
5. **跑绿**：CI 全绿，提交（`feat: …`）。
6. **重构**：整理结构，测试全程保持绿，提交（`refactor: …`）。**重构 commit 禁止改动公开签名与任何测试文件**——要改签名走变更协议；要改测试，那就不叫重构。

补充规则：

- 模块较大时可切更小批次，但每一批都必须完整走完先红后绿。
- 编排层测试的断言对象与众不同：mock 掉核心层与适配层后，断言「谁被以什么顺序、什么参数调用」以及「某步失败时是否按合同中止/重试/回滚」；计算结果对不对归核心层测试管。E 类异常场景的主战场通常就在这里。
- **证据以 CI 为准**：红的权威证据 = 红回放 job 对标记红用例的通过记录；绿的权威证据 = CI 全绿运行历史。agent 本地贴图只作过程参考。
- 人眼轨模块：正常实现后交付截图或预览链接，同步记入 `status.json` 与 `99-进度.md` 本模块小节，等待关卡③验收。
- 强化/对抗档模块转绿后，夜间变异测试若发现存活变异 = 弱断言债务，走 B 级补测试或书面豁免。
- 对抗档模块实行**三方独立**：写测试、写实现、攻击方为三个不同 agent；攻击 agent 只依据合同构造攻击输入，其找到的任何失败即无可辩驳的违约，进反例回收。
- 进度文件按模块分节更新，各 agent 只改自己小节。

### 防作弊铁律（写码 agent 与初审 agent 都必须知晓）

- 禁止先写实现再补测试（commit 顺序 + commit 范围纯净性 + 红回放三重审计）
- 禁止越过停点：反问清单未获答复、合同未冻结前，同一次运行不得产出后续阶段的任何产物
- 禁止为通过而弱化测试：删断言、放宽断言、skip / only、修改期望值迁就错误实现；除 test commit 或批准的 B 级反例修复外，feat/fix/refactor commit 不得触碰测试文件
- 禁止硬编码应付测试：针对测试输入写死返回值（性质测试的随机输入 + 初审边界抽查双重拆穿）
- 禁止兜底绕过：try/catch 吞错、默认值顶替、检测测试环境走特殊分支
- 测试失败时只有两条合法出路：**改实现**，或**走变更协议改合同**。不存在第三条路。

## 阶段 4：模块初审（初审 agent）

机械检查已全部由 CI 承担。初审 agent 只做**三道判断题**，产出中文报告：

1. **漏测判断**：对照 `01-接口.md` 或 `interfaces/<模块名>.md` 方法清单、场景编号与不变量编号，找出未被测试覆盖的项。
2. **反硬编码抽查**：新写 1–2 个 fixture 示例之外的边界用例并实际运行。
3. **断言强度抽查**：中文描述与断言是否等强——描述写「应得 20」而断言只查「大于 0」即为弱断言，打回。

**报告纪律：每一项结论必须附证据引用**（文件路径 + 行号，或 CI 运行链接）。**无证据 = 未审**，报告打回。

通过后更新 `status.json` 与 `99-进度.md` 中该模块小节。

## 阶段 5：集成测试 + 人眼验收【关卡③】

- **时机**：功能的所有模块通过初审后，拉起独立的审核 agent。
- **换真货**：拆除 mock，各层真实现串联运行。mock 之间各自自洽但整体跑不通，是接口先行最常见的翻车点，**此阶段不可省略**。
- 逐条执行编号验收场景，报告格式：`S1 → 通过/失败 → 详情 + 归因初判（断链在哪层哪模块、疑似违反哪条合同）`。编号与需求文档一一对应，缺号即漏测。
- **场景固化**：验收通过的编号场景沉淀为自动化集成/E2E 测试进入 CI 场景回归——一次性人跑变成永久防线，此后任何 PR 弄断旧功能会立即被拦。
- **人眼验收**：主导者实际打开界面按场景操作一遍（或看截图/录屏），验收人眼轨模块；通过后录制基线截图，交给视觉回归自动看守，改版时更新基线。
- **报告全绿 + 界面过眼 = 功能完成**，更新 `status.json` 与 `99-进度.md`；出现红色则回到对应阶段修复。

## 反例回收协议（零容忍）

对抗体系的成果处理规则，任何来源适用——性质测试反例、夜间轰炸、变异存活、集成场景红、线上 bug：

1. **可复现即必修**：凡带输入或种子记录的失败一律必修。禁止忽略、禁止靠重试蒙混、禁止 skip 掩盖。性质测试每次 CI 换新随机种子，框架自动记录失败种子，因此其失败天然可复现。
2. **反例冻结**：失败输入最小化后存入 `fixtures/counterexamples/`，对应回归测试标注 `反例#N`，永久保留；原则上 append-only，删除或改写必须附变更提案。
3. **天然接入 B 级微流程**：反例本身就是「先红」的重现测试，修到绿即闭环；若证明的是不变量或合同本身写错，升 A 级改合同。
4. **暂不可复现的失败**（无种子记录的环境抖动）登记在进度文件观察区；同类第二次出现即升级为必修债务。

## 变更协议（分级）

### 级别 A —— 合同 / 架构变更（全流程）

1. **停下**，不写任何绕过代码
2. 提交**变更提案**：改什么 / 为什么 / 影响哪些文档、测试、模块
3. 等主导者批准
4. 按序修改并各自走文档 PR：架构文档（如涉及层边界）→ 共享模型/需求 → 接口文档 → 规划 → 测试 → 实现
5. 变更 commit 注明提案编号

### 级别 B —— bug 修复 / 反例清偿（微流程）

这是 TDD 最经典的用法，同时防住「声称修好了但其实没修」：

1. **先写重现失败的测试**（bug 重现用例，或反例回收生成的用例），CI 跑红——证明问题真实存在且已被捕捉
2. 修复至绿；反例测试永久保留
3. CI 机械检查通过即可合并，无需人工关卡
4. 修复过程中发现根源是合同错了 → 升级为级别 A

### 级别 C —— 纯展示调整（样式、文案、布局微调）

直接修改，人眼验收并更新视觉回归基线，不强制测试。**禁止顺手改动任何逻辑代码**，一旦涉及逻辑即升级为 B 或 A。

### 探针例外

对外部行为不确定之处，允许先写一次性探针代码验证。**探针的输出存入 `fixtures/contract/` 成为正式数据源**；探针代码本身验证完即弃，禁止合入主干。禁止在真空里设计合同，也禁止拿探针代码当实现。

## 微调点清单（每个新项目开工前过一遍）

- [ ] **层级**：按开工四问增删层、改层名
- [ ] **推进模式**：本期走蓝图还是增量；蓝图期先定功能范围（requirements-index 初稿），范围外的想法进候补区不进本期
- [ ] **项目识别口径**：什么算新项目、旧项目、旧项目中的新模块；旧项目中新增大功能默认按旧项目处理
- [ ] **术语表**：初始化本项目的领域词
- [ ] **违禁词表**：如有外部系统，列出其原始字段黑名单
- [ ] **测试框架与运行命令**：Vitest / Jest / pytest …，写明一键运行命令
- [ ] **性质测试与组合工具**：fast-check / hypothesis；PICT 或等价物
- [ ] **CI 平台与守门脚本**：默认 GitHub Actions，按仓库补齐 PR 门禁、红回放标记规则、commit 范围审计、状态一致性校验与定时任务
- [ ] **治理开关**：CODEOWNERS（含守门设施自护、状态文件放行路径与生成缓存放行路径）/ 分支保护；不可用则启用 tag 降级方案
- [ ] **验证强度阈值**：把「高错误代价判定」实例化为本项目清单，按项目风险调整强化/对抗档触发线
- [ ] **示例实例化**：判断工具四问各附本项目示例；模板所有〈占位〉处以本项目领域内容填充
- [ ] **变异测试**：运行时机（夜间/每周）与存活率要求
- [ ] **夜间轰炸预算**：时长或迭代数
- [ ] **功能分级判据**：什么算轻量道
- [ ] **原始需求封存方式**：用户原话、附件、对话补充如何写入 `00-原始需求.md`，是否只追加
- [ ] **代码冲突扫描范围**：旧项目接口层必须检查哪些目录、入口、历史功能、测试和配置
- [ ] **实现方式决策模板**：从零实现、修改现有代码、复用扩展、重构后实现、旁路替换的判据
- [ ] **大需求拆分阈值**：什么情况下从单份 `01-接口.md` 拆为 `interfaces/<模块名>.md`，以及何时同步拆 `conflicts/<模块名>.md`（模块数、行数、并行度、外部协议、复用标准）
- [ ] **fixture 保鲜频率**：按外部系统变化速度定
- [ ] **人眼轨形式**：截图 / 本地预览 / 录屏；视觉回归基线管理方式
- [ ] **反问答复通道**：文档内清单批量答 / PR 评论逐条答 / 对话答后由 agent 写回，选一种固定
- [ ] **hooks 策略**：是否启用 `core.hooksPath scripts/hooks`；哪些检查只提醒，哪些检查阻断
- [ ] **状态文件策略**：是否启用 `workflow-state.json` / `status.json`；schema 字段、状态枚举、证据链接格式
- [ ] **并行度**：单 agent 串行，还是冻结后多 agent 按模块并行
- [ ] **初审严格度**：小项目可省反硬编码抽查，「报告必须附证据」的纪律保留
