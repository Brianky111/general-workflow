interface:
  display_name: "General Workflow"
  short_description: "Anchor-first staged solution and feature delivery"
  default_prompt: "Use $general-workflow to classify solution scope before feature merging, maintain staged construction and aggregate progress when required, then close one Delivery Anchor gap through the real production path."

policy:
  allow_implicit_invocation: true
