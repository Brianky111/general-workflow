interface:
  display_name: "General Workflow"
  short_description: "Staged dev workflow: detect stage, load stage doc, orchestrate executors"
  default_prompt: "Use $general-workflow to inspect the repository, detect the current workflow stage, keep the main thread as orchestrator, and delegate scoped execution to subagents when useful."

policy:
  allow_implicit_invocation: true
