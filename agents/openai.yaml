interface:
  display_name: "General Workflow"
  short_description: "Anchor-first delivery across solutions and features"
  default_prompt: "Use $general-workflow to frame any required cross-feature solution without duplicating feature truth, then close one Delivery Anchor gap at a time through the real production path with finite verification."

policy:
  allow_implicit_invocation: true
