interface:
  display_name: "General Workflow"
  short_description: "Vertical feature workflow with TDD, test matrix, and DoD"
  default_prompt: "Use $general-workflow to inspect the repository, route the current stage, and advance the scoped work with the required contracts, test matrix, TDD, integration, and completeness evidence."

policy:
  allow_implicit_invocation: true
