interface:
  display_name: "General Workflow"
  short_description: "Implementation-first BDD/TDD with existing-code reuse"
  default_prompt: "Use $general-workflow to freeze the smallest sufficient behavior contract, reuse the repository's real code and test paths, and continue through implementation and verification without unnecessary document gates."

policy:
  allow_implicit_invocation: true
