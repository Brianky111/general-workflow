interface:
  display_name: "General Workflow"
  short_description: "Vertical feature workflow with BDD, TDD, matrix, and DoD"
  default_prompt: "Use $general-workflow to route the current stage and advance the scoped work through BDD examples, contracts, a Feature Test Matrix, TDD, integration, and completeness evidence."

policy:
  allow_implicit_invocation: true
