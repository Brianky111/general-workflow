# 变更记录

接手审阅（2026-09-10）产生的三条，一行一条按顺序追加，编号不复用也不删除。backlog 里 deferred 或
dropped 的 A-ID，note 指向这里的 C-ID。

| C-ID | date | 来源 | 变更 | 受影响 | 代价 |
| --- | --- | --- | --- | --- | --- |
| C-01 | 2026-09-10 | 用户审阅 | A-04（作废直接改状态）改为作废必须留痕，由 A-10 承接 | A-04 dropped；A-10 新增；billing/service.py 的 void | 多一张 audit 表和一次迁移；旧行为不再受保护 |
| C-02 | 2026-09-10 | 用户审阅 | A-09（每日摘要邮件）不要了 | A-09 dropped；scripts/daily_digest.py | 删 cron 另立 A-ID，本批不做；在那之前脚本继续存在但不受保护 |
| C-03 | 2026-09-10 | 接手基线 | A-08（夜间对平）无环境可调，未受保护 | A-08 deferred；D-01 | 对平规则改坏了没有测试会红，直到有脱敏快照为止 |

## C-01

- 决定：改行为，不是改旧行。A-04 留在验收表作记录，backlog 标 dropped 指向本条；新行为编号 A-10，等认领。
  审阅答案原话：「作废必须留痕，现在直接改 status 的做法不能保。」
- 代价：多一张 audit 表和一次迁移；A-04 的旧行为从今天起不受保护，改它不再需要保护表里有它。
- 验收：A-10 在 docs/requirements.md 的验收场景表。

## C-02

- 决定：不要了。财务从没看过这封邮件，是三年前写工具的人替他们想的。
- 代价：删掉 cron 和脚本是一次改代码，另立 A-ID 走功能切片，本批不做；在那之前 scripts/daily_digest.py
  继续存在但不受保护，config.py 里的明文凭据随它一起进队列。
- A-09 留在验收表，backlog 标 dropped 指向本条。

## C-03

- 决定：延后基线，不是取消行为。用户审阅答「不知道」，按保留处理；但脚本要生产库快照才能跑，本机和 CI
  都没有，调不了就没有基线。
- 代价：A-08 未受保护——改到 scripts/nightly_reconcile.py 或 billing 的表结构时，没有任何测试会因为对平
  规则被改坏而变红。这是已接受的风险，登记为 D-01。
- 重开条件：拿到一份脱敏快照，或财务说清对平规则能写成特征测试。届时跑基线，A-08 进 S-00。
