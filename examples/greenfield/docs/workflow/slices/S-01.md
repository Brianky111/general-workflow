# Slice S-01: 按日期区间导出订单明细 CSV
- owner: alice
- claimed: 2026-09-09 / a1b2c3d
- stage: 8 真实代码实现
- write_scope: src/report; tests/report
- architecture_hypothesis: H-01

## Slice map
- user_outcome: 运营自己敲一条命令拿到订单明细 CSV，不再提工单等工程师跑 SQL
- requirements: must「按日期区间导出 CSV」、must「区间参数非法时拒绝并说明」
- acceptance: A-01, A-02
- real_entrypoint: CLI `report-export`，由 pyproject 的 console_scripts 注册
- path:
  1. 输入与身份：解析 --from / --to / --out；无身份概念，能连副本即可用
  2. 校验：日期可解析、from 不晚于 to、输出路径可写且不覆盖已有文件
  3. 应用用例：`export_orders(range, sink)`
  4. 领域规则：区间按闭区间，边界日包含在内（依赖 D-01 的时区结论）
  5. 数据：psycopg 服务端游标读只读副本，全程不写库
  6. 结果：写出固定表头的 CSV，stdout 打印行数，退出码 0
- failure_paths: from 晚于 to（A-02）退出码 2 并说明是哪个参数；连不上副本时退出码 3 并给出连接串主机名，不打印口令
- data_changes: 无。这条切片只读，不建表不迁移
- observability: stdout 打印区间与行数；异常打印到 stderr 并带退出码，不做日志文件
- test_evidence: `tests/report` 单元测试覆盖区间校验与 CSV 写出；一条集成测试用种子副本跑完整 CLI
- deploy_and_rollback: `pip install -e .` 装到运营机器，`pip uninstall report-export` 撤回；本版不打包分发
- out_of_scope: xlsx 导出（A-04，已延后，见 docs/changes.md 的 C-01）；空区间提示（A-03，留给下一条切片）

## Acceptance
| A-ID | status | evidence |
| --- | --- | --- |
| A-01 | delivered | `report-export --from 2026-09-01 --to 2026-09-07 --out day.csv` → 退出码 0，day.csv 为表头加 3 行，金额与副本一致 @ a1b2c3d |
| A-02 | in-slice | - |

## Blockers
- D-01（created_at 时区）没结论之前，A-01 的边界日断言按本地时间写。结论翻转就要重跑这条
  evidence，不是改断言凑绿。
